{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Ukemi's main window: pick a repository folder, run the bundled jj, hand a path to the file manager, the default app or a terminal, and let the toolbar move and zoom the window the way a title bar does.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "core:window:allow-start-dragging",
    "core:window:allow-toggle-maximize",
    "opener:allow-reveal-item-in-dir",
    {
      "identifier": "opener:allow-open-path",
      "allow": [
        { "path": "**" },
        { "path": "**", "app": "Terminal" }
      ]
    }
  ]
}
